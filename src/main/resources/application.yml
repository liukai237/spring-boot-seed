server:
  port: 8989
  tomcat:
    uri-encoding: UTF-8
  compression:
    enabled: true

spring:
  application:
    name: spring-seed
  profiles.active: dev
  http:
    encoding:
      charset: UTF-8
      enabled: true
      force: true
  jackson:
    default-property-inclusion: non_empty

mybatis:
  type-aliases-package: com.yodinfo.seed.domain
  mapper-locations: classpath*:mapper/**/*.xml

mapper:
  mappers: com.yodinfo.seed.common.BasicMapper, com.yodinfo.seed.common.CommonMapper
  not-empty: true
  identity: mysql
  use-simple-type: false

pagehelper:
  helperDialect: mysql
  reasonable: true
  supportMethodsArguments: true
  params: count=countSql

logging:
  level:
    root: WARN
    org.springframework.web: DEBUG
    com.yodinfo.seed.dao: DEBUG

jasypt:
  encryptor:
    password: Bda7bXaExAA
    property:
      prefix: "Yod@["
      suffix: "]"

---
spring:
  profiles: dev
  datasource:
    url: jdbc:mysql://mysql.iakuil.com:3306/kai
    username: Yod@[YcKTF9RyttwYKyzRGfIDfw==]
    password: Yod@[SVJAoJSsCcRsKQnxkvBrt7PJBqn4rb69]
    type: com.zaxxer.hikari.HikariDataSource
    hikari:
      driver-class-name: com.mysql.cj.jdbc.Driver

logging:
  level:
    root: INFO

---
spring:
  profiles: prod